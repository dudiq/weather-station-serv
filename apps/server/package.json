{
  "name": "@local-weather/server",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "clean:dist": "rimraf dist ./.turbo",
    "clean:node_modules": "rimraf ./node_modules",
    "dev": "NODE_ENV=development dotenv-run -v -- tsx watch --inspect --clear-screen=false ./src/app.ts",
    "build": "dotenv-run -- tsup",
    "start": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "@repo/build-version": "workspace:*",
    "@repo/errors": "workspace:*",
    "@repo/result": "workspace:*",
    "date-fns": "^4.1.0",
    "ms": "^2.1.3"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/ts-config": "workspace:*",
    "@types/node": "^24.3.1",
    "@types/ms": "^2.1.0"
  }
}
